import type { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "com.gaspro.agency",
  appName: "GasPro Agency",
  webDir: "out",
  server: { androidScheme: "https" }
};

export default config;
